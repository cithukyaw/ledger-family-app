import {FC} from "react";
import {Box} from "@mui/material";

const ServerError: FC = () => {
  return <Box className="center-content">Some data could not be load because of server error.</Box>
}

export default ServerError;
